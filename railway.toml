[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python service/scripts/server.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
